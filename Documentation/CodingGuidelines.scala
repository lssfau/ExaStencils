

// add empty brackets to method calls without arguments
// add empty brackets to method declarations without arguments

// always add a return type to a function's signature _unless_ the function is reasonably short (<= 2 lines)

// avoid using the return keyword if it is not necessary

// don't use curly braces for one-line/ inline functions
  override def pp (out : ...) = out << "foo"

// avoid using new keyword / avoid overloading the this() operator
// -> overload apply method of companion objects instead


